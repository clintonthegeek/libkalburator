build/
build-*/
.worktrees/
.claude/
compile_commands.json
*.swp
*.o
*.so
*.a
.clangd/
.cache/

# In-source CMake build artifacts (builds belong in build/ — see CLAUDE.md)
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
Makefile
_deps/
.qt/
Testing/

# opencode agent tooling (machine-local; not project context)
opencode.json
.lcm/

# Microsoft Graph experiment (credentials, token caches, captured payloads — never commit)
msgraph*/

# Google Calendar/People experiment (OAuth client secrets, token caches,
# captured payloads / real user data — never commit)
google/

